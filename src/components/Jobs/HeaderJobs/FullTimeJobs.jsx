import React from "react";
import JobPostingForm from "./JobApplication";

const FullTimeJobs = ({ onClose }) => {
  return (
    <JobPostingForm onClose={onClose} />
  );
};

export default FullTimeJobs;
